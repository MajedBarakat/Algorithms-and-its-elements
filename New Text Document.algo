ALGORITHM algorithm_name
VAR
    x:CHAR
    a,b,c:INTEGER
BEGIN
    read (x)
    a:=0
    b:=1
    c:=0
    WHILE (x <> '.') DO
        a=a+1
        
        IF (x=" ") THEN
        b=b+1
        END_IF
        
        IF (x="a" or x="e" or x="i" or x="y" or x="u") THEN
        c+1
        END_IF

    END_WHILE
END